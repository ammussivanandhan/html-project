function reorder(clickedCard) {const firstCard = document.querySelector('.big');if (clickedCard === firstCard) return;clickedCard.classList.add('swap-active');firstCard.classList.add('swap-active');setTimeout(() => {const tempImg = firstCard.querySelector('img').src;firstCard.querySelector('img').src = clickedCard.querySelector('img').src;clickedCard.querySelector('img').src = tempImg;const tempInfo = firstCard.querySelector('.product-info').innerHTML;firstCard.querySelector('.product-info').innerHTML = clickedCard.querySelector('.product-info').innerHTML;clickedCard.querySelector('.product-info').innerHTML = tempInfo;clickedCard.classList.remove('swap-active');firstCard.classList.remove('swap-active');}, 150); }
const track = document.querySelector('.carousel-track');
const cards = document.querySelectorAll('.carousel-card');
const nextBtn = document.querySelector('.nextBtn');
const prevBtn = document.querySelector('.prevBtn');
let index = 0;function getCardWidth() {return cards[0].getBoundingClientRect().width;}function updateCarousel() {const moveX = getCardWidth() * index;track.style.transform = `translateX(-${moveX}px)`;}nextBtn.addEventListener('click', () => {index++;if (index >= cards.length) index = 0;updateCarousel();});prevBtn.addEventListener('click', () => {index--;if (index < 0) index = cards.length - 1; updateCarousel();});window.addEventListener('resize', updateCarousel);updateCarousel();